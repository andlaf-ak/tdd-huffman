/// Shared constants for bit stream operations
/// Number of bits in a byte
pub const BITS_PER_BYTE: usize = 8;

/// Mask for extracting the most significant bit (leftmost bit)
/// Binary: 10000000
pub const MSB_MASK: u8 = 0x80;
