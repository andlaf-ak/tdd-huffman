fn main() {
    println!("TDD Huffman Compression");
    // make sure that files with a size that cannot be stored in 4 bytes are rejected
    // remove the "examples" directoy
    // check if the way tests are structured is aligned to Rust standards
}
