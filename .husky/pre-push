#!/usr/bin/env sh

# Exit on any command failure
set -e

# Run full test suite (unit, property, and e2e tests) before pushing
echo "🧪 Running all tests before push..."
make test

echo "✅ All tests passed! Ready to push."
